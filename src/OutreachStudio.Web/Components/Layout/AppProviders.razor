@* Every interactive page renders these inside its own island, because the layout is static and the
   editor page runs in WebAssembly where a server island's providers cannot reach it. *@

<MudPopoverProvider />
<MudDialogProvider CloseButton="true" Position="DialogPosition.Center" MaxWidth="MaxWidth.ExtraSmall" />
<MudSnackbarProvider />
