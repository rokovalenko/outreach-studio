@* Pages carry their own render mode, so the popover, dialog and snackbar hosts get an interactive
   island of their own in the layout. They share the circuit with the page and therefore its services. *@

<MudPopoverProvider />
<MudDialogProvider CloseButton="true" Position="DialogPosition.Center" MaxWidth="MaxWidth.ExtraSmall" />
<MudSnackbarProvider />
